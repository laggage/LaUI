<Page x:Class="UITest.View.ButtonDemo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UITest.View"
      xmlns:control="clr-namespace:LaUI.Control;assembly=LaUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ButtonDemo">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Margin="10" 
                       HorizontalAlignment="Center"
                       Text="IconToggleButton" />
            <control:IconToggleButton Content="This is IconToggleButton"
                                      Padding="10"
                                      Margin="20 0" Icon="this is Icon"/>
        </StackPanel>
    </Grid>
</Page>
